# 🗑️ Хроніки Помийки (Dumpster Chronicles)

> *Перша у світі соціальна сміттєва гача в Telegram.*

**@DumpsterChroniclesBot** — це бот, який перетворює ваші нудні чати на постапокаліптичні біоми, а ваших друзів — на епічні колекційні картки. Всі арти та лор генеруються штучним інтелектом (Gemini 3.0) у реальному часі.

![Logo](https://github.com/user-attachments/assets/placeholder-logo.png)

## ⚡ Особливості

* **🧬 ШІ-Архітектор (Gemini 3.0 Flash)**: Бот аналізує поведінку користувачів у чаті та створює для них унікальні "Blueprint" картки. Токсичний? Отримай біом **🔥 Вогняний** і високу Атаку. Любиш кодити? Твій біом — **🤖 Техно**.
* **🎨 Nano Banana Pro (Gemini 3.0 Pro Image)**: Генерація високоякісних артів у єдиному стилі "Cyberpunk Trash Fantasy".
* **🌍 Система Біомів**: Кожен чат Telegram має свій унікальний "Біом" (обчислюється математично від ID чату). Картки падають тільки у відповідних біомах.
* **⚡ Race Conditions**: Механіка "Хто перший встав — того і капці". Дропи карток обробляються через Redis з атомарними Lua-скриптами, щоб уникнути подвійного клейму.
* **🐍 Python 3.14**: Використовуємо найсвіжіші фічі мови та `google-genai` SDK v1.56+.

## 🛠 Технічний Стек

* **Мова**: Python 3.14
* **Фреймворк**: aiogram 3.x
* **База даних**: PostgreSQL 17 (Async SQLAlchemy + Alembic)
* **Кеш & Локи**: Redis (для FSM та обробки Race Conditions)
* **AI**: Google Gemini 3.0 Flash (Thinking Mode) + Gemini 3 Pro Image
* **Інфраструктура**: Docker & Docker Compose

## 🚀 Встановлення та Запуск

Вам знадобиться встановлений **Docker** та **Docker Compose**.

1.  **Клонуйте репозиторій:**
    ```bash
    git clone [https://github.com/your-username/pomyyka-the-game.git](https://github.com/your-username/pomyyka-the-game.git)
    cd pomyyka-the-game
    ```

2.  **Налаштуйте змінні оточення:**
    Скопіюйте приклад конфігурації:
    ```bash
    cp .env.example .env
    ```
    Відкрийте `.env` та заповніть:
    * `BOT_TOKEN`: Отримайте у @BotFather.
    * `GEMINI_API_KEY`: Отримайте в Google AI Studio.
    * `ADMIN_USER_IDS`: Ваш Telegram ID (через кому, якщо декілька).

3.  **Запустіть проект:**
    ```bash
    docker-compose up --build -d
    ```

    *Бот автоматично накатить міграції бази даних при старті.*

## 🎮 Команди

### Для Адмінів (God Mode)
* `/newcard` — Ручний режим створення картки (Wizard: Ім'я -> Арт -> Стати).
* `/autocard` (у відповідь на повідомлення) — **Магія**. Бот читає історію юзера, вигадує лор і створює картку автоматично.

### Для Гравців (Сміттярів)
* `/start` — Реєстрація у грі.
* `/me` або `/profile` — Перегляд профілю та балансу.
* `/inventory` — Твоя колекція сміттєвих скарбів.
* **Дропи**: Просто сиди в чаті. Якщо пощастить, з'явиться аномалія. Тисни кнопку **"✋ Хапнути"** швидше за інших!

## 📂 Структура Проекту

* `services/ai_architect.py` — "Мізки" (генерація параметрів картки).
* `services/art_forge.py` — "Кузня" (генерація зображень).
* `services/redis_lock.py` — Атомарна логіка клейму дропів.
* `database/models.py` — Схема БД (Users, Cards, Chat Logs).
* `handlers/` — Обробники команд Telegram.

## 📜 Ліцензія

MIT License. Копайтесь у коді на здоров'я, але не забувайте мити руки.